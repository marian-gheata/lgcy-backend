const status = ['public', 'secret'];
const notificationTypes = {
  COMMENT: 'comment',
  MSG: 'message',
  POST: 'post',
  TIMELINE: 'timeline',
};

module.exports = {
  notificationTypes,
  status,
};
